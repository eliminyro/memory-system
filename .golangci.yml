# golangci-lint v2 config. The CI workflow pins the binary version (v2.12.2);
# this file makes the enabled linter set explicit so a local `golangci-lint run`
# matches CI. `standard` = errcheck, govet, ineffassign, staticcheck, unused.
version: "2"
linters:
  default: standard
