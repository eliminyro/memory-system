# Usage-weighted ranking benchmark — results

> **STATUS: NOT YET RUN.** This file is a placeholder committed with the
> harness. The matrix and verdict below are populated by section 4 of the
> `phase-b-usage-benchmark` change, running the harness against a disposable
> Postgres. Numbers here are absent, not results.

- Embedder: `fake` (deterministic), dim 768
- Seed: 42
- Weights: 0, 0.5, 1, 2, 3, 5
- Noise (FP=FN): 0, 0.1, 0.2, 0.35, 0.5

## Verdict

**PENDING** — not yet run; regenerated by `go test -tags usagebench -run TestUsageBench ./benchmarks/usagebench/`.

The verdict is computed from the numbers by `ComputeVerdict` (design D5): **GO**
iff the gap-workload nDCG@10 lift at FP=FN=0.2 reaches +0.03 over the weight-0
baseline **and** the no-gap control degrades by no more than 0.01 at that weight;
otherwise **NO-GO**.
